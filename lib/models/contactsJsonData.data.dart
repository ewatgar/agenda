import 'package:agenda/models/agenda.data.dart';
import 'package:shared_preferences/shared_preferences.dart';

class ContactsJsonData {
  Future saveJsonData(AgendaData agenda) {
    final prefs = await SharedPreferences
  }
}
